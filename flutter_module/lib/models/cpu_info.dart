class CPUInfo {
  double? maxFrequency;
  double? minFrequency;

  /// For example, 4 x 1.90 GHz | 4 x 2.46 GHZ
  String? cpuInfo;

  CPUInfo(Map<String, Object?>? map) {
    if (map != null) {
      // Unpack data from map
    }
  }
}
